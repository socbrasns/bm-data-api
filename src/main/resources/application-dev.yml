application:
  title: "Benchmarker API for Benchmarker WebApp"
  version: "v1.0"
logging:
  level:
    root: INFO
    web: INFO

server:
  port: 8090
  error:
    include-message: always
    include-binding-errors: always
    whitelabel:
      enabled: true
spring:
  application:
    name: bm-data-api
  mvc:
    pathmatch:
          matching-strategy: ANT_PATH_MATCHER
  datasource:
    driverClassName: org.h2.Driver
    password: ''
    url: jdbc:h2:mem:bmapidb;DB_CLOSE_DELAY=-1
    username: sa
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    defer-datasource-initialization: true
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    

jwt:
  expiration: 15 #in minutes
  secret: "cfgsecret"
  
rememberme:
  expiration: 1296000 # 15 days
  secret: "cfgsecret"
  key: "remember-me"

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include : '*'